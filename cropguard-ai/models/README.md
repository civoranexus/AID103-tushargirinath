# AI Models

This directory will contain trained deep learning models
for crop disease detection using plant leaf images.


Training the model in google colab using GPU

Finished the training phase—now moving on to designing the frontend layout and UI, and integrating the frontend with the backend.
